---
Назва: Контекст проєкту teatralo4ka.odesa.ua
Тип: персональний шар інструкцій v8
Дата оновлення: 2026-08-09
---

# Контекст проєкту: teatralo4ka.odesa.ua

Сайт театральної студії: публічні сторінки українською й англійською плюс адмін-панель на Firebase. Цей файл читається **перед** застосуванням пакету стандартів (`sveltekit-canon/selection_criteria/v8/`).

## Базові параметри

| Параметр | Значення |
|----------|----------|
| Профіль | **static** |
| Adapter | `@sveltejs/adapter-static`, fallback `404.html` |
| Хостинг | GitHub Pages з власним доменом |
| Origin | `https://teatralo4ka.odesa.ua` |
| `base` | `''` — **власний домен, не підпапка** |
| Порт dev-сервера | 5173 (типовий, не перевизначено) |
| Спільний origin з іншими застосунками? | **Ні** — власний домен |
| STORAGE_PREFIX | `teatralo4ka_` (`src/lib/config/storage.ts`) |

> Префікс сховища лишається обов'язковим попри окремий домен: він коштує нічого і страхує від переїзду на спільний хостинг. Формально це той випадок, коли [STORAGE-NAMESPACE-v8](../sveltekit-canon/selection_criteria/v8/STORAGE-NAMESPACE-v8.md) називає префікси «рекомендованими», а не критичними.

## Прийняті рішення

| Питання | Обрано | Причина | Дата |
|---------|--------|---------|------|
| Бібліотека i18n | `svelte-i18n` + типізована `schema.ts` | Динамічне завантаження словників; ключі типізовані вручну | до 2026-08 |
| Мови | `uk` (типова), `en` | Обидві вичитані людиною — машинних перекладів немає | до 2026-08 |
| Мовні адреси | префікс шляху `/en/…`, `uk` на голому шляху | Реалізовано в `generate-sitemap.ts`; маршрути мовного сегмента ще немає | до 2026-08 |
| Санітизація HTML | `isomorphic-dompurify` | Працює і в браузері, і під час prerender — звичайний `dompurify` там падає | до 2026-08 |
| Валідація даних | `zod` (`src/lib/schemas/`) | Контент із Firestore валідується перед показом | до 2026-08 |
| Інструмент аналітики | GA4 | — | 2026-08-08 |
| Згода на збір даних | **без банера** | Особистий/некомерційний проєкт, автор свідомо приймає ризик щодо ЄЕЗ | 2026-08-08 |
| Середовище компонентних тестів | ще не обрано | Компонентних тестів немає; при появі — рішення за [CODE-QUALITY-v8 § 4.1](../sveltekit-canon/selection_criteria/v8/CODE-QUALITY-v8.md) | — |
| Модель версіонування | бамп вручну через `npm run bump-version` | `husky` не встановлено, авто-bump не діє | до 2026-08 |
| Стратегія testid у production | **B — зберігати** | Плагіна видалення немає; отже де-факто B, і це записано, а не вдається за A | 2026-08-09 |

## Реєстр кастомних вимірів аналітики

| Параметр | Область | Створено | Що означає |
|----------|---------|----------|------------|
| — | — | — | Кастомних вимірів не зареєстровано |

## Обрані optional-файли пакету

- [x] `I18N-v8.md` — uk + en
- [x] `ANALYTICS-v8.md` — GA4 `G-W9XXERE0RJ`, без банера
- [x] `VERSIONING-v8.md` — версія показується користувачам
- [x] `DEPENDENCIES-v8.md` — Firebase у клієнтському бандлі
- [ ] `AUTH-FORM-v8.md` — **не застосовується**: канон описує публічну форму з реєстрацією та відновленням пароля; тут лише вхід адміністратора без самореєстрації
- [ ] `DEPLOY-ENVIRONMENTS-v8.md` — **не застосовується**: одне середовище, dev-превʼю не потрібне
- [ ] `OBSERVABILITY-v8.md` — відкладено до появи E2E у CI
- [ ] `DOCUMENTATION-v8.md` — `.private/docs/` порожня
- [ ] `DEBUGGING-v8.md` — є `errorLogger`, кнопки збору логів немає

## Що не перевіряється автоматично

Кожен рядок — борг. Список має скорочуватися.

| Правило | Чому перевірки немає | План |
|---------|----------------------|------|
| E2E-сценарії, axe-аудит | Playwright у проєкті не встановлено | Окрема зміна разом із кроком `playwright install` у CI |
| Рантайм-дублікати `data-testid` | Те саме — потрібен браузер | Разом із Playwright |
| Інваріанти зібраного HTML (canonical, порожні сторінки) | Скрипт над `build/` ще не написано | Окрема зміна |
| Паритет ключів перекладу | Тесту немає | Окрема зміна |
| `npm audit` | Кроку в CI немає | Окрема зміна |
| CSP | Не налаштована взагалі (`svelte.config.js` без `csp`) | Окрема зміна; домен GA додати одночасно |

## Легасі-зони та списки винятків

| Список | Де живе | Записів | Причина |
|--------|---------|---------|---------|
| Правила ESLint у режимі `warn` | `eslint.config.js` | 4 правила / 224 місця | Разова зміна на сотні місць непереглядна; кожне мігрується окремим комітом |
| `svelte/require-each-key` | там само | 60 | Keyed each — HIGH; міграція потребує вибору ключа для кожного списку |
| `svelte/no-navigation-without-resolve` | там само | 72 | Правило плагіна, якого немає в стандарті; спершу рішення, чи це канон проєкту |
| `@typescript-eslint/no-explicit-any` | там само | 84 | Скорочується поступово |
| `svelte/prefer-svelte-reactivity` | там само | 8 | `Map`/`Set` у стані → `SvelteMap`/`SvelteSet` |
| Точкові `eslint-disable` | 3 місця в коді | 3 | Кожен із причиною поруч; два — хибні спрацювання правил |

## Свідомі відхилення від пакету

| Правило пакету | Відхилення | Обґрунтування |
|----------------|-----------|---------------|
| `PROJECT-STRUCTURE-v8 § 7` — межа розміру файлу | `admin/settings/+page.svelte` 2078 рядків, `HeaderSection.svelte` 1396, `ArticleForm.svelte` 1347 | Розділення заплановане, але це рефакторинг з ризиком регресій в адмінці; робиться окремо від решти змін |
| `CODE-QUALITY-v8 § 6.2` — покриття ≥ 70 % | Покриття не міряється | Тести є лише на схеми, сховище, логер і санітайзер; поріг увімкнеться після появи тестів на контролери |

## Локальні пастки проєкту

- **Порт 5173 типовий і не перевизначений.** Спільний із рештою проєктів екосистеми: якщо запустити тести іншого проєкту, поки живий цей dev-сервер, вони мовчки перевірять чужий сайт.
- **`base: ''`, а не `/teatralo4ka/`.** Сайт на власному домені, тож шляхи прямі — на відміну від сусідніх проєктів у підпапках GitHub Pages.
- **Адмінка потрапляє у збірку** (`build/admin/…`, 2 сторінки) і зараз не закрита від індексації.
- **Контент валідується перед збіркою** (`npm run validate-content` у `prebuild`): зламаний markdown у `src/lib/i18n/pages/` завалить білд, і це навмисно.
- **Firebase у клієнтському бандлі.** Ключі публічні за визначенням; безпека тримається на правилах Firestore, а не на приховуванні конфігу.

## Проєктні конвенції поза пакетом

- Контент сторінок — markdown у `src/lib/i18n/pages/<lang>/`, з frontmatter (`status`, `priority`, `changefreq`), звідки `generate-sitemap.ts` будує `sitemap.xml`.
- Налаштування головної сторінки (порядок блоків, віджети) зберігаються у Firestore і редагуються в адмінці — звідси розмір `admin/settings/+page.svelte`.
- Workflow збирає матрицю «шкіл» (`VITE_SCHOOL_ID`), зараз у ній один запис. Структура залишена під майбутнє розширення на кілька сайтів з однієї кодової бази.
