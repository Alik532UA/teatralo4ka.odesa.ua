/**
 * Старі адреси новин, які тепер ведуть на нові.
 *
 * ## Навіщо окремий модуль на одну мапу
 *
 * Її читають ТРИ місця, і в жодному з них не можна тримати її саму:
 *
 *   • `routes/news/[id]/+page.ts` — щоб стара адреса пререндерилася сторінкою
 *     перенаправлення;
 *   • `config/redirects.ts` — щоб та сторінка не поїхала в `sitemap.xml`
 *     порожньою (SEO-v9 називає це CRITICAL) і щоб її не обходили перевірки,
 *     розраховані на звичайні сторінки;
 *   • `scripts/generate-sitemap.ts` через той самий `redirects.ts`, а він
 *     виконується в Node через tsx, де аліасів SvelteKit немає.
 *
 * Останнє й вирішує: тримати мапу в `config/codeNews.ts` не можна, бо той
 * модуль імпортує `$app/paths`, і скрипт мапи сайту на ньому впав би. Тому тут
 * — модуль без жодного імпорту.
 *
 * ## Чому це ПРЕРЕНДЕР, а не мовчазний клієнтський перехід
 *
 * Причина в самому проханні автора: «можливо хтось вже відправляв в соц.
 * мережах це помилкове посилання». Той, хто розкриває посилання в соцмережі,
 * JavaScript не виконує — він бачить рівно ту розмітку, яку віддав сервер. Тож
 * стара адреса мусить бути СТОРІНКОЮ з `meta refresh`, і саме тому вона є в
 * `entries()`.
 *
 * Решта перейменованих адрес — вистав, фестивалю, груп, майстрів і випускників
 * — довго була вирішена інакше: їх не пререндерили, покладаючись на те, що
 * `fallback: '404.html'` віддасть оболонку, а на нову адресу поведе клієнтський
 * роутер. Заміряно 7 вересня 2026: не веде НІКУДИ, усі 57 адрес давали 404.
 * Тепер вони працюють так само, як ця, і живуть в одному реєстрі —
 * `config/renamedAddresses.ts`.
 */
export const RENAMED_NEWS_IDS: Record<string, string> = {
	/*
	 * 2026-09-04. Слаг був транслітерацією й називав СІМНАДЦЯТЬ студентів —
	 * обидві причини перейменування прохання автора називає прямо: «18 студентів
	 * і назву не транслітерацією а англійською». Число в адресі й підвело: коли
	 * знайшовся вісімнадцятий, адреса почала суперечити власному заголовку.
	 */
	'30-y-sezon-i-17-studentiv-2026': '2026-year-30th-season-18-students'
};
