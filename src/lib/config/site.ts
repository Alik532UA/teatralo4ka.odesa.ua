/**
 * Адреса сайту — одне джерело правди (CUSTOM-DOMAIN-v8 § 1, SEO-v8 § 1.2).
 *
 * До 2026-08-16 цей рядок жив у трьох місцях незалежно: `+layout.ts`,
 * `+layout.svelte` і `scripts/generate-sitemap.ts` — плюс четвертий раз у
 * `static/robots.txt`. Переїзд на інший домен — операція атомарна за
 * визначенням: половина змін дає стан гірший за повну їх відсутність (canonical
 * веде на стару адресу, sitemap — на нову, і пошуковик бачить два дублі сайту).
 * Чотири незалежні копії роблять цю атомарність питанням уважності.
 *
 * Окремо про гейти: CUSTOM-DOMAIN-v8 § 5 називає HIGH саме випадок, коли
 * перевірка над `build/` дублює origin із приміткою «мусить збігатися з…». Не
 * збігається вона рівно тоді, коли це важливо, — після переїзду, — і тоді гейт
 * оголошує чужою КОЖНУ адресу сайту. Тому `generate-sitemap.ts` читає значення
 * звідси, а не тримає власну копію.
 *
 * БЕЗ хвостової косої риски: усі споживачі додають шлях, який починається зі
 * слеша (`${SITE_ORIGIN}${url.pathname}`). Інваріант `src/site-origin.test.ts`
 * тримає обидві властивості — і форму, і те, що копій більше немає.
 */
export const SITE_ORIGIN = 'https://teatralo4ka.odesa.ua';

/**
 * Абсолютна адреса сторінки.
 *
 * Саме абсолютна, і саме від константи: під час prerender `page.url.origin`
 * дорівнює `sveltekit-prerender`, а `base` з `$app/paths` відносний — обидва
 * вже давали в цьому проєкті `https://teatralo4ka.odesa.ua../logo/…`
 * у структурованих даних (SEO-v8 § 1.2–1.3).
 */
export const siteUrl = (pathname = '/'): string => `${SITE_ORIGIN}${pathname}`;
