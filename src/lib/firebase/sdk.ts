/**
 * Рівно ті функції Firestore, які потрібні публічній частині сайту — і нічого
 * більше.
 *
 * ## Навіщо перелік, коли можна `import('firebase/firestore')`
 *
 * Динамічний імпорт ЦІЛОГО пакета робить живими всі його експорти: Rollup
 * більше не знає, чого ніхто не кличе, і трясти дерево нема за що. Заміряно
 * 2026-09-11 на тій самій збірці: увесь клієнтський JS 723 → 751 КБ, тобто
 * лінивість коштувала б 28 КБ ваги, яку доти прибирало саме дерево.
 *
 * Тому лінивою стає не бібліотека, а ЦЕЙ модуль: він реекспортує перелічене
 * поіменно, тож дерево трясеться так само, як при звичайному імпорті, а
 * `await import()` у `lazy.ts` тримає його поза критичним шляхом.
 *
 * Новій функції Firestore місце тут, а не в місці виклику: інакше поруч
 * зʼявиться другий динамічний імпорт, і чанків стане два (`check-bundle-budget`
 * це ловить).
 */
export {
	collection,
	doc,
	getDoc,
	getDocs,
	limit,
	orderBy,
	query,
	serverTimestamp,
	setDoc,
	where
} from 'firebase/firestore';

/*
 * Через `$lib`, а не `./config`: юніт-тести підміняють конфіг заглушкою за
 * шаблоном із хвостом `/firebase/config` у `vitest.config.ts`, і відносний
 * запис під нього не підпадає — тести піднімали б справжній Firebase.
 */
export { auth, db } from '$lib/firebase/config';
