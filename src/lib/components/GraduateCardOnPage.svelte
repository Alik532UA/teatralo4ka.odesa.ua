<script lang="ts">
	import GraduateCard from './GraduateCard.svelte';
	import { closeGraduateModal, graduateFromPageState } from '$lib/services/graduateModal.svelte';

	/**
	 * Картка випускника, що відкривається НА ЦІЙ сторінці.
	 *
	 * ## Навіщо окремий компонент на три рядки
	 *
	 * Обличчя в рядках складу — посилання на сторінку людини, і доти натискання
	 * вело туди по-справжньому: сторінка викладача зникала, а після закриття
	 * картки читач опинявся в галактиці й мусив шукати викладача заново. Потік
	 * учнів на тій самій сторінці поводився інакше — відкривав картку поверх, — і
	 * різниця між ним і рештою облич не мала жодного пояснення.
	 *
	 * Щоб обличчя відкривалися на місці, сторінці потрібні дві половини: сам
	 * `GraduateAvatarRow` перехоплює натискання, а хтось мусить намалювати картку
	 * за станом сторінки. Друга половина — це рівно три рядки, і саме тому вона
	 * тут: тричі скопійовані три рядки розійшлися б прапорцем `showGalaxyLink`
	 * або обробником закриття, і на одній зі сторінок картка лишалася б без ходу
	 * назад у галактику.
	 *
	 * `showGalaxyLink` увімкнений завжди: цей компонент за визначенням стоїть ПОЗА
	 * галактикою — на самій галактиці картку малює її власна сторінка, зі своїм
	 * кешем анкет.
	 */
	const graduate = $derived(graduateFromPageState());
</script>

<GraduateCard showGalaxyLink {graduate} onclose={closeGraduateModal} />
